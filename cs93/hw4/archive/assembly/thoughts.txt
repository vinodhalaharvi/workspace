        
# read a character
check if input is ready by inspecting the control memory location
        load the control memory location address to a register
                la $t0, control #control memory address
                lw $t1, ($t0) # control data
        load the control memory location data to a register
                beq $t1, 0x0
        do alu operation to check if the control indicates read ready
if the input is ready then copy the buffer memory location to a register

